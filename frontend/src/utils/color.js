function hexToHsl(hex) {
  const r = parseInt(hex.slice(1, 3), 16) / 255;
  const g = parseInt(hex.slice(3, 5), 16) / 255;
  const b = parseInt(hex.slice(5, 7), 16) / 255;
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  let h, s;
  const l = (max + min) / 2;

  if (max === min) {
    h = s = 0;
  } else {
    const d = max - min;
    s = l > 0.5 ? d / (2 - max - min) : d / (max + min);
    switch (max) {
      case r: h = (g - b) / d + (g < b ? 6 : 0); break;
      case g: h = (b - r) / d + 2; break;
      default: h = (r - g) / d + 4; break;
    }
    h /= 6;
  }

  return { h: h * 360, s: s * 100, l: l * 100 };
}

function hslToHex(h, s, l) {
  s /= 100;
  l /= 100;
  const k = (n) => (n + h / 30) % 12;
  const a = s * Math.min(l, 1 - l);
  const f = (n) => l - a * Math.max(-1, Math.min(k(n) - 3, Math.min(9 - k(n), 1)));
  const toHex = (x) => Math.round(x * 255).toString(16).padStart(2, '0');
  return `#${toHex(f(0))}${toHex(f(8))}${toHex(f(4))}`;
}

/**
 * Deriva os tons de upload/ping a partir da cor de download escolhida pelo
 * usuário, clareando e dessaturando progressivamente (mesma relação visual
 * das famílias Tailwind ex. blue-500 -> blue-300 -> blue-200).
 */
export function deriveShades(baseHex) {
  const { h, s, l } = hexToHsl(baseHex);
  return {
    download: baseHex,
    upload:   hslToHex(h, Math.max(s - 15, 20), Math.min(l + 22, 92)),
    ping:     hslToHex(h, Math.max(s - 30, 10), Math.min(l + 34, 95)),
  };
}
